[project]
name = "clutch-studio"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "numpy",
    "opencv-python-headless",
]

[tool.setuptools.packages.find]
include = ["studio*"]
